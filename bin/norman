#!/usr/bin/env node

var norman = require('..');

var procfile = process.argv[2];

var concurrency = process.argv[4];

norman.createFormation(procfile, function(formation) {
  formation.scale(concurrency);
});
